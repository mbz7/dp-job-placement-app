import React from "react";

function JobDetail({ jobs, client }) {
  // console.log(jobs);
  // console.log(client);
  return (
    <div>
      <h1>JobDetail</h1>
    </div>
  );
}

export default JobDetail;
